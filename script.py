import test

radius = 2

circumference = test.calc_circumference(radius)
area = test.calc_area(radius)

print('For a circle of radius '+str(radius)+', the circumference is '+str(circumference)+' and the area is '+str(area)+'.')
